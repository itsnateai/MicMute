# PR Notes — claude/cloud-implementation-prompt-cxsd7

> **Asus cleanup:** After creating the PR and verifying the build, delete this file from the repo and commit: `git rm PR_NOTES.md && git commit -m "chore: remove PR_NOTES.md after PR created"`. Consider adding `PR_NOTES.md` to `.gitignore` if Cloud will use this pattern again.

**Title:** fix: version mismatch, static GUID buffers in InitMicEndpoint and EnumCaptureDevices

## Summary
- Fixed header comment version "1.8.0" → "1.8.2" to match `g_version`
- Made GUID `Buffer()` allocations in `InitMicEndpoint` and `EnumCaptureDevices` static to prevent repeated allocation
- Updated CHANGELOG.md and CLAUDE.md for v1.8.2

## Implementation Details
- `MicMute.ahk` line 3: header version string corrected from "1.8.0" to "1.8.2"
- `MicMute.ahk` line 38: `g_version` bumped from "1.8.1" to "1.8.2"
- `InitMicEndpoint()`: Three `Buffer(16)` GUID allocations wrapped in `static` + `_guidsInit` guard. In degraded mode (no mic), `SyncMuteState` calls this every 5s — leaked 48 bytes/cycle without static.
- `EnumCaptureDevices()`: Same pattern applied — 3× `Buffer(16)` + 1× `Buffer(20)` PKEY now static. Not a hot path but consistent with the fix above.

## Assumptions
- No assumptions — all decisions verified against codebase.

## Files Changed
- `MicMute.ahk` — Fixed header version, bumped g_version, made GUID/PKEY buffers static in both functions
- `CHANGELOG.md` — Added v1.8.2 section documenting fixes
- `CLAUDE.md` — Updated version status line

## Testing / Verification
- Re-read all modified files to confirm correctness
- Traced `InitMicEndpoint` call chain: startup (line 81), `SyncMuteState` (line 379), `ReinitMic` (line 434), `SelectDevice` (line 1023)
- Traced `EnumCaptureDevices` call chain: `PopulateDeviceMenu` (lazy-loaded on first right-click)
- Confirmed `static` pattern is correct AHK v2 (static locals persist across calls, initialized once)
- Cannot compile — **needs compile verification by Asus**

## Checklist
- [x] Read all files before editing
- [x] Followed existing codebase patterns
- [x] All imports resolve to real packages
- [x] Error handling on all external calls
- [x] Resource cleanup verified (no leaks)
- [x] Hot-path allocation check passed (no allocations in timers/loops)
- [x] CHANGELOG.md updated with version bump
- [x] CLAUDE.md updated
- [x] One logical change per commit

Generated by Claude Code Cloud
